import 'package:flutter/material.dart';

class SamosaScreen extends StatelessWidget {
  const SamosaScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Samosa Screen"),
      ),
    );
  }
}
