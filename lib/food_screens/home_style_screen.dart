import 'package:flutter/material.dart';

class HomeStyleScreen extends StatelessWidget {
  const HomeStyleScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Home Style"),
      ),
    );
  }
}
