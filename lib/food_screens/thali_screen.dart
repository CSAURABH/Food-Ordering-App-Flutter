import 'package:flutter/material.dart';

class ThaliScreen extends StatelessWidget {
  const ThaliScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Thali Screen"),
      ),
    );
  }
}
