import 'package:flutter/material.dart';

class FriesScreen extends StatelessWidget {
  const FriesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Fries Screen"),
      ),
    );
  }
}
