import 'package:flutter/material.dart';

class SandwichScreen extends StatelessWidget {
  const SandwichScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Sandwich Screen"),
      ),
    );
  }
}
